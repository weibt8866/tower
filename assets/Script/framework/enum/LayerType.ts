/**
 * 界面层级
 */
enum LayerType
{
    floorLayer, //底层
    viewLayer,  //界面层
    menuLayer,  //菜单层
    popupLayer, //弹窗层
    popMenuLayer, 
    messageLayer, //提示层
    guideLayer,  //引导层
    toolTipLayer, 
    waitingLayer, //等待层
    loadingLayer, //加载层
    systemPopLayer, 
    debugLayer
}
